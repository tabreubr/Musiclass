spring:
  application:
    name: musiclass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never
jwt:
  secret: ${JWT_SECRET:musiclass-super-secret-key-2024-change-this-in-production}
  expiration: ${JWT_EXPIRATION:86400000}